[
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 345,
      "end_byte": 351,
      "start_line": 12,
      "start_col": 9,
      "end_line": 12,
      "end_col": 15
    },
    "message": "encrypted write to `stored` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  },
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 441,
      "end_byte": 447,
      "start_line": 16,
      "start_col": 9,
      "end_line": 16,
      "end_col": 15
    },
    "message": "encrypted write to `stored` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  }
]
