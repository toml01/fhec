[
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 496,
      "end_byte": 497,
      "start_line": 19,
      "start_col": 9,
      "end_line": 19,
      "end_col": 10
    },
    "message": "encrypted write to `a` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  },
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 516,
      "end_byte": 517,
      "start_line": 20,
      "start_col": 9,
      "end_line": 20,
      "end_col": 10
    },
    "message": "encrypted write to `b` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  },
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 535,
      "end_byte": 539,
      "start_line": 21,
      "start_col": 9,
      "end_line": 21,
      "end_col": 13
    },
    "message": "encrypted write to `flag` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  }
]
