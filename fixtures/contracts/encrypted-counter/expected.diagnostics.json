[
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 436,
      "end_byte": 441,
      "start_line": 20,
      "start_col": 9,
      "end_line": 20,
      "end_col": 14
    },
    "message": "encrypted write to `count` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  },
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 543,
      "end_byte": 549,
      "start_line": 23,
      "start_col": 9,
      "end_line": 23,
      "end_col": 15
    },
    "message": "encrypted write to `frozen` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  },
  {
    "code": "FHE4002",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 676,
      "end_byte": 689,
      "start_line": 28,
      "start_col": 9,
      "end_line": 28,
      "end_col": 22
    },
    "message": "a `view` function cannot grant ACL access to its encrypted return value; the caller must have been granted access elsewhere",
    "rule": "§8.4"
  },
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 765,
      "end_byte": 770,
      "start_line": 32,
      "start_col": 9,
      "end_line": 32,
      "end_col": 14
    },
    "message": "encrypted write to `count` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  },
  {
    "code": "FHE4001",
    "severity": "warning",
    "span": {
      "file": "input.fsol",
      "start_byte": 956,
      "end_byte": 961,
      "start_line": 40,
      "start_col": 9,
      "end_line": 40,
      "end_col": 14
    },
    "message": "encrypted write to `count` targets a state variable, which has no key at all, so its owner is not provably `msg.sender`; the sender grant is withheld here, so the transaction sender does not gain read access to a ciphertext that is not provably its own. Add an explicit grant if that is what you intend",
    "rule": "§8.1"
  }
]
