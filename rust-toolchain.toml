[toolchain]
# Pinned so new stable clippy lints land as scheduled maintenance, not as
# surprise CI breakage on unrelated branches. Bump deliberately.
channel = "1.98"
components = ["rustfmt", "clippy"]
