[package]
name = "fhec-verify"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "solc runner (standard JSON) + error remapping"

[dependencies]
log = "0.4"
semver = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
