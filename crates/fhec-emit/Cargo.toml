[package]
name = "fhec-emit"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "patcher, renderer, source maps, idempotence guard"

[dependencies]
fhec-ir = { path = "../fhec-ir" }
fhec-syntax = { path = "../fhec-syntax" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
